package com.xlbiz.incident.agent.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration for AI services.
 * Spring AI auto-configuration handles ChatClient setup based on application.yml configuration.
 */
@Configuration
public class AiConfig {
    // Spring AI 0.8.0 auto-configures ChatClient based on application.yml
    // No manual bean configuration needed
}
